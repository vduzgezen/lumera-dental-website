# =============================================================================
# Lumera Dental Portal - Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values for your environment.
# NEVER commit your .env file to version control.

# =============================================================================
# Database
# =============================================================================
# For development, use SQLite:
# DATABASE_URL="file:./dev.db"
#
# For production, use a PostgreSQL connection string:
# DATABASE_URL="postgresql://user:password@host:5432/database?schema=public"
DATABASE_URL="file:./dev.db"

# =============================================================================
# Authentication
# =============================================================================
# IMPORTANT: In production, this MUST be set to a secure random string (32+ chars)
# The application will fail to start in production if JWT_SECRET is not set.
# Generate one with: openssl rand -base64 32
JWT_SECRET="your-super-secret-jwt-key-here"

# =============================================================================
# Email (Resend)
# =============================================================================
# Get your API key from https://resend.com
# This is used to send welcome emails to new users with their credentials.
RESEND_API_KEY="re_your_api_key_here"
EMAIL_FROM="Lumera Dental <hello@yourdomain.com>"

# =============================================================================
# Production Checklist
# =============================================================================
# Before deploying to production, ensure you have:
# 
# 1. [ ] Set a strong JWT_SECRET (32+ random characters)
# 2. [ ] Configured DATABASE_URL for your production database
# 3. [ ] Set up Resend API key for email functionality
# 4. [ ] Verified EMAIL_FROM uses your verified domain
# 5. [ ] Run `npx prisma migrate deploy` to apply migrations
# 6. [ ] Set NODE_ENV=production
